let Stage = require('../Scripts/Stage');
describe('Stage', function () {
  beforeEach(function () {
  });
  describe('unit tests', function () {
    it('should actually exist.', function () {
      throw new Error('they don\'t.');
    });
  });
});

