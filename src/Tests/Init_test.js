let Init = require('../Scripts/Init');
describe('Init()', function () {
  describe('handleTouchScreen()', function () { });
  describe('checkBrowserSupport()', function () { });
  describe('checkProtocol()', function () { });
  describe('setStyle()', function () { });
  describe('bypass()', function () { });
});
