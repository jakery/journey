let Game = require('../Scripts/Game');
describe('Game', function () {
  describe('setLevelClock()', function () { });
  let game = new Game();
  describe('returnToTitle()', function () { });
  describe('setDeadMessage()', function () { });
});
